server {
    listen 80;
    server_name bugs;

    access_log logs/bugs.access.log main;

    include sso/handler;

    location / {
        proxy_pass http://localhost:20300;
        include include/proxy_params;
        include sso/protect;
    }
}
